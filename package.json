{
  "name": "manage-data-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Node.js API backend for the Manage Data Panel assignment using Supabase PostgreSQL.",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "check": "node --check src/config.js && node --check src/supabaseClient.js && node --check src/validation.js && node --check src/routes/records.js && node --check src/app.js && node --check src/server.js && node --check api/index.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.57.4",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "zod": "^3.23.8"
  },
  "engines": {
    "node": ">=22"
  }
}
